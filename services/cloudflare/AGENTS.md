# Cloudflare working instructions

Read the root AGENTS.md and docs/workflow.md first. This directory separates
working research (`README.md`, `docs/`) from published `content/`.

Read `docs/research.md` before changing claims. M2 coverage and pass order are
in the root docs/plan.md; the owner's scope list is in docs/features.md.
Verify product names, capabilities, local development and applicable plan
limits from current official sources. Cite each claim and date product and
limits verification separately. Record gaps rather than guessing. Keep the
service `draft` until the owner approves its complete coverage.
