---
/**
 * Shared local-development note for Cloudflare storage products: the
 * programmatic Miniflare harness plus a pointer to the Workers runtime page.
 */
---

<p>
  For a local test harness, the <a
    href="https://developers.cloudflare.com/workers/testing/miniflare/get-started/"
    >Miniflare JavaScript API</a
  > lets you configure Worker code and local bindings and dispatch requests from Node.js.
  See <a href="/cloudflare/workers/#local-development"
    >Workers local development</a
  > for the underlying workerd runtime; service emulation supplies the binding behavior.
</p>
